export enum BillingPlan {
    NONE = "NONE",
    ONE_TIME = "ONE_TIME",
    RECURRING = "RECURRING",
}

export enum SubscriptionInterval {
    NONE = "NONE",
    MONTHLY = "MONTHLY",
    YEARLY = "YEARLY",
}
